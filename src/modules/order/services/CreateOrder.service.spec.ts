import { CreateOrderService } from './CreateOrder.service'

let sut: CreateOrderService

describe('CreateOrderService', async () => {
  beforeEach(async () => {
    sut = new CreateOrderService()
  })

  it.skip('should ...', async () => {
    // Arrange
    // Act
    // Assert
  })
})
