import { CloseOrderGateway } from './CloseOrder.gateway'

let sut: CloseOrderGateway

describe('CloseOrderGateway', async () => {
  beforeEach(async () => {
    sut = new CloseOrderGateway()
  })

  it.skip('should ...', async () => {
    // Arrange
    // Act
    // Assert
  })
})