import { CollaboratorPresenter } from './CollaboratorPresenter'

let sut: CollaboratorPresenter

describe('CollaboratorPresenter', async () => {
  beforeEach(async () => {
    sut = new CollaboratorPresenter()
  })

  it.skip('should ...', async () => {
    // Arrange
    // Act
    // Assert
  })
})