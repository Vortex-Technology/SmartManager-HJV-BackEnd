import { OwnersPrismaMapper } from './OwnersPrismaMapper'

let sut: OwnersPrismaMapper

describe('OwnerPrismaMapper', async () => {
  beforeEach(async () => {
    sut = new OwnersPrismaMapper()
  })

  it('should ...', async () => {
    // Arrange
    // Act
    // Assert
  })
})
