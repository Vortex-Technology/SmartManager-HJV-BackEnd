import { ApiKeysPrismaMapper } from './ApiKeysPrismaMapper'

let sut: ApiKeysPrismaMapper

describe('ApiKeysPrismaMapper', async () => {
  beforeEach(async () => {
    sut = new ApiKeysPrismaMapper()
  })

  it('should ...', async () => {
    // Arrange
    // Act
    // Assert
  })
})